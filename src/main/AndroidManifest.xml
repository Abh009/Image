<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.teambhargavinilayam.abh.image">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" android:label="@string/image1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondActivity" android:label="@string/image2"/>
        <activity android:name=".ThirdActivity"  android:label="@string/image3"/>
        <activity android:name=".FourthActivity" android:label="@string/image4"/>
        <activity android:name=".FifthActivity"  android:label="@string/image5"/>
        <activity android:name=".SixthActivity"  android:label="@string/Done"/>
    </application>

</manifest>